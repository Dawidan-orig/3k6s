Подразумевает, что данные перекидываются и подготавливаются из диска и на пространство ядра, и на пространство пользователя.

Позволяет, вроде как, контролировать логику изменения любых данных, я плохо понял, Танненбаум надушнил.

Используется в основном в графике, чтоб там ничего не мерцало, так как мы берём из первого буфера предыдущую часть изображения, а из второго - только изменённую, и накладываем одно на другое.