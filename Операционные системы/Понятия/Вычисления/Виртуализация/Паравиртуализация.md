Берём ОС и переписываем Ядро, чтобы выполнять все возможные команды в защищённом [[Режимы ядра|режиме]], начхав на обычный.

Пример: ЭВМ, которая поднимает Windows под Linux, использует паравиртуализацию.