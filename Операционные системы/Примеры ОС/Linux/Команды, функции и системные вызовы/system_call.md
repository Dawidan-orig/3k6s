Переводит функцию в пространство ядра. А точнее - мы передаём ей номер системной функции, вызывая её таким образом.
Кроме того, она проверяет корректность переданного ей системного номера.

Для передачи параметров:
- По старинке - в лоб загрузить параметры прямо в регистры процессора.
- {А больше я и не нашёл}

У системных функций, как мы помним, больший доступ к ядру. Они могут даже в ВАП влезть. Потому и существует вызов через эту вот функцию - так мы гарантируем, что ничего не сломаем.

Добавление новой системной функции (Почти никогда не нужно делать):
1) Делаем новый элемент в конце таблицы системных функций
2) Для всех поддерживаемых аппаратных платформ определяем в файле номер функции
3) Компиляция, помещение функции в образ ядра.